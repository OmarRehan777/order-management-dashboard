import type { User } from "../types/user";
import mockUsers from "./mockUsers";

const mockCurrentUser: User = mockUsers[0]; // Simulate the currently logged-in user (e.g., the cashier)

export default mockCurrentUser;
